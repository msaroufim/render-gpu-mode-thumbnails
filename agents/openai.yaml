interface:
  display_name: "GPU MODE YouTube Thumbnails"
  short_description: "Render branded 1280×720 speaker thumbnails"
  default_prompt: "Use $render-gpu-mode-thumbnails to make a GPU MODE YouTube thumbnail from a title, speaker names, and headshots."
