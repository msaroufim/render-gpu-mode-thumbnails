interface:
  display_name: "GPU MODE YouTube Thumbnails"
  short_description: "Render thumbnails or process speaker intake"
  default_prompt: "Use $render-gpu-mode-thumbnails to create a GPU MODE thumbnail or process the private speaker-intake queue."
